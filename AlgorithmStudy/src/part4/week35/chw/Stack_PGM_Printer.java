package part4.week35.chw;

public class Stack_PGM_Printer {

}
/*

def solution(priorities, location):
    answer = 0
    arr = [lo for lo in range(len(priorities))]
    while True:
        max_task = max(priorities)
        idx = arr.pop(0)
        fir_task = priorities.pop(0)
        if fir_task == max_task :
            answer += 1
            if idx == location:
                break
        else:
            arr.append(idx)
            priorities.append(fir_task)
    return answer


 */

